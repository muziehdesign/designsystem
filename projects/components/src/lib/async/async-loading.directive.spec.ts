import { MzAsyncLoading } from './async-loading.directive';

describe('AsyncLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new MzAsyncLoading();
    expect(directive).toBeTruthy();
  });
});

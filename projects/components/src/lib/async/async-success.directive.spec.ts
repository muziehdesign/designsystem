import { MzAsyncSuccess } from './async-success.directive';

describe('AsyncSuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new MzAsyncSuccess();
    expect(directive).toBeTruthy();
  });
});

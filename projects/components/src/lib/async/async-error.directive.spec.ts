import { MzAsyncError } from './async-error.directive';

describe('AsyncErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new MzAsyncError();
    expect(directive).toBeTruthy();
  });
});

import { MzModalClose } from './modal-close.directive';

describe('ModalCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new MzModalClose();
    expect(directive).toBeTruthy();
  });
});
